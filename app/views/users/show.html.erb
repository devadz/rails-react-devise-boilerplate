<div class="col-md-10 col-md-offset-1">
  <h1>Profile for <%= avatar_for(@user, size: 50) %> <%= @user.username %></h1>

  <%= link_to "Edit profile", edit_user_path(@user), :class => "btn btn-default" %>
</div>
